[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "g0rd0n"
version = "0.1.0"
description = "A resource-bounded scientific search for alternative cognitive architectures"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = []

[tool.setuptools.packages.find]
include = ["g0rd0n*"]

[tool.unittest]
start-directory = "tests"
